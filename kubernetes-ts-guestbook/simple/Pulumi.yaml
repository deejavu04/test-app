# Important Limitation
# This works fine for demos, assignments amd single environment projects.
# But not ideal for real environments because: all stacks share the same config with no environment separation

name: guestbook
runtime: nodejs
description: Kubernetes Guestbook example based on https://kubernetes.io/docs/tutorials/stateless-application/guestbook/
config:
  isMinikube:
    description: Whether you are deploying to minikube
    default: true