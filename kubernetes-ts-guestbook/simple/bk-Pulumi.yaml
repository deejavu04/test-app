# name: guestbook
# runtime: nodejs
# description: Kubernetes Guestbook example based on https://kubernetes.io/docs/tutorials/stateless-application/guestbook/
# template:
#   config:
#     isMinikube:
#       description: Whether you are deploying to minikube
#       default: true

# Important Limitation
# This works fine for:
# learning
# demos
# assignments
# single environment projects

# But not ideal for real environments because:
# all stacks share the same config
# no environment separation
# harder to manage prod/dev differences but works

# dejia@TripleAce:~/mytest/kubernetes-ts-guestbook/simple$ pulumi config
# KEY         VALUE
# isMinikube  true

name: guestbook
runtime: nodejs
description: Kubernetes Guestbook example based on https://kubernetes.io/docs/tutorials/stateless-application/guestbook/
config:
  isMinikube:
    description: Whether you are deploying to minikube
    default: true